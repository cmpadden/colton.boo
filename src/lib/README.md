# Experiment assets

`ghost-model.ts` contains the first renderable asset: a deliberately small,
procedural emoji ghost mesh. The body is a lathed surface with smooth normals;
the face coordinates are exported separately so eyes and mouth can be rendered
as dark, emissive primitives over a translucent body.
